import Contact from "../../Components/Contact";

export const metadata = {
  title: "Contact",
  description: "Contact Form is here",
};

const Page = () => {
  return <Contact />;
};

export default Page;
